/**
 * Shared test setup. Core tests run in node; react tests opt into jsdom via a
 * `// @vitest-environment jsdom` docblock. jsdom shims (layout / rects /
 * observers) go here as react view slices land — kept empty until then.
 */
export {};
