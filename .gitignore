node_modules/
dist/
*.tsbuildinfo
*.log
.DS_Store
.turbo/
coverage/
.import-bak-*/

# Storybook workbench (component-seams ticket 08)
storybook-static/
# Ignore .tests/ CONTENTS (not the dir itself, so git can descend to the negation below).
.tests/*
# …except the committed VR baselines (component-seams ticket 14 — the first catalog wave
# baselined the seam; baselines are tracked, diff output + transient runner artifacts are not).
!.tests/vr/
.tests/vr/**/__diff_output__/
